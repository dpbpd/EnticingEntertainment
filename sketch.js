function setup() {
    //createCanvas(windowWidth,windowHeight);
}
  
function draw() {
    createCanvas(windowWidth,windowHeight);
    background('#76b0b6');     //('#7cb4ba'); ('#76b0b6');
    ellipse(windowWidth / 2, windowHeight / 2, windowWidth / 3, windowHeight / 3);
}